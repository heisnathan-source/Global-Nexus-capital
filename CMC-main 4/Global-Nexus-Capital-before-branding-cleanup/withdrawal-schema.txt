========== DATABASE / SCHEMA FILES ==========
./db/schema.sql
./db/patch_2026_08_24_final_audit.sql
./withdrawal-schema.txt

========== WITHDRAWAL AMOUNTS REFERENCES ==========
./FIXES_APPLIED.md:17:- Withdrawal amount choices are loaded from Admin-configured withdrawal amounts.
./FIXES_APPLIED.md:18:- Withdrawal service charge and amount received calculate from Admin policy when available.
./README.md:257:- Gross withdrawal amount is reserved from the wallet when submitted.
./README.md:258:- Admin approval completes the withdrawal; rejection refunds the reserved gross amount.
./PART_5_WITHDRAWAL_SECURITY.md:11:- Withdrawal amount choices remain Admin-configured.
./MONEY_AUDIT.md:11:- withdrawal amount enforcement
./lib/transaction-rules.js:14:  if (!Number.isInteger(amountPesewas) || amountPesewas <= 0) return "Invalid withdrawal amount.";
./lib/transaction-service.js:88:export async function createWithdrawal({ userId, amount, fee, metadata = {} }) {
./app/api/withdrawal-options/route.js:5:export async function GET(request){const s=session(request);if(!s)return Response.json({error:"Unauthorized"},{status:401});const db=getDb();const amounts=(await db.query("SELECT amount FROM withdrawal_amounts WHERE active=TRUE ORDER BY display_order,amount")).rows.map(r=>Number(r.amount));const p=await getWithdrawalPolicy(s.userId);return Response.json({amounts,allowed:p.allowed,reason:p.allowed?null:p.reason,feeMode:p.feeMode||null,feeValue:Number(p.feeValue||0),processingMinHours:Number(p.processingMinHours||0),processingMaxHours:Number(p.processingMaxHours||48),rankName:p.rankName||null});}
./app/api/withdrawals/route.js:25:   return Response.json({error:"Enter a valid withdrawal amount."},{status:400});
./app/api/withdrawals/route.js:55:    SELECT 1 FROM withdrawal_amounts
./app/api/withdrawals/route.js:60:    throw new Error("This withdrawal amount is not currently available.");
./app/api/withdrawals/route.js:64:  if(net<=0) throw new Error("Withdrawal amount after fee must be greater than zero.");
./app/api/withdrawals/route.js:95:  `,[s.userId,value,fee,`WD-${order.rows[0].id}`,JSON.stringify({withdrawalOrderId:order.rows[0].id,netAmount:net})]);
./app/api/withdrawals/route.js:100:    VALUES($1,$2,'withdrawal_hold',$3,$4,'Withdrawal amount reserved')
./app/components/WithdrawalFlow.jsx:9: async function load(){try{const [a,o,v]=await Promise.all([fetch('/api/account'),fetch('/api/withdrawal-options'),fetch('/api/account-security')]);const ad=await a.json(),od=await o.json(),vd=await v.json();if(a.ok)setHasFundsPassword(!!ad.account?.has_funds_password);if(o.ok){setOptions(od.amounts||[]);setPolicy(od);}if(v.ok){setIdentity(vd.verification||null);setGovName(vd.verification?.government_name||"");setGovId(vd.verification?.government_id_number||"");}}catch(e){setError("Unable to load withdrawal settings.")}}
./app/components/WithdrawalFlow.jsx:13: async function submit(e){e.preventDefault();setError("");setBusy(true);try{const r=await fetch('/api/withdrawals',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({amount,fundsPassword:password})});const d=await r.json();if(!r.ok)throw new Error(d.error);setPassword("");router.refresh();alert(`Withdrawal submitted. Reference: WD-${d.order.id}`)}catch(e){setError(e.message)}finally{setBusy(false)}}
./app/components/WithdrawalFlow.jsx:17: return <><form className="form-card" onSubmit={submit}><label>Choose amount</label><div className="amount-grid">{options.map(v=><button type="button" className={`amount-choice ${String(v)===String(amount)?'selected':''}`} key={v} onClick={()=>setAmount(String(v))}>GHS {Number(v).toLocaleString()}</button>)}</div>{!options.length&&<p className="muted">No withdrawal amounts are currently configured by Admin.</p>}<label>Funds Password<input value={password} onChange={e=>setPassword(e.target.value.replace(/\D/g,'').slice(0,6))} inputMode="numeric" maxLength={6} type="password" placeholder="6 numbers"/></label><div className="service-row"><span>Service charge</span><strong>GHS {feeValue.toFixed(2)}</strong></div><div className="service-row"><span>You receive</span><strong>GHS {net.toFixed(2)}</strong></div>{!policy?.allowed&&policy?.reason&&<p className="auth-error">{policy.reason}</p>}{error&&<p className="auth-error">{error}</p>}<button type="submit" className="primary-button" disabled={busy||!amount||password.length!==6||policy?.allowed!==true}>{busy?'Submitting…':'Submit Withdrawal'}</button></form><p className="muted">Processing time: {policy?.processingMinHours??0}–{policy?.processingMaxHours??48} hours.</p></>;
./.audit-backup-withdrawals/user-withdrawals-route.js.bak:25:   return Response.json({error:"Enter a valid withdrawal amount."},{status:400});
./.audit-backup-withdrawals/user-withdrawals-route.js.bak:55:    SELECT 1 FROM withdrawal_amounts
./.audit-backup-withdrawals/user-withdrawals-route.js.bak:60:    throw new Error("This withdrawal amount is not currently available.");
./.audit-backup-withdrawals/user-withdrawals-route.js.bak:64:  if(net<=0) throw new Error("Withdrawal amount after fee must be greater than zero.");
./.audit-backup-withdrawals/user-withdrawals-route.js.bak:95:  `,[s.userId,value,fee,`WD-${order.rows[0].id}`,JSON.stringify({withdrawalOrderId:order.rows[0].id,netAmount:net})]);
./.audit-backup-withdrawals/user-withdrawals-route.js.bak:100:    VALUES($1,$2,'withdrawal_hold',$3,$4,'Withdrawal amount reserved')
./.audit-backup-warnings/WithdrawalFlow.jsx.bak:9: async function load(){try{const [a,o,v]=await Promise.all([fetch('/api/account'),fetch('/api/withdrawal-options'),fetch('/api/account-security')]);const ad=await a.json(),od=await o.json(),vd=await v.json();if(a.ok)setHasFundsPassword(!!ad.account?.has_funds_password);if(o.ok){setOptions(od.amounts||[]);setPolicy(od);}if(v.ok){setIdentity(vd.verification||null);setGovName(vd.verification?.government_name||"");setGovId(vd.verification?.government_id_number||"");}}catch(e){setError("Unable to load withdrawal settings.")}}
./.audit-backup-warnings/WithdrawalFlow.jsx.bak:13: async function submit(e){e.preventDefault();setError("");setBusy(true);try{const r=await fetch('/api/withdrawals',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({amount,fundsPassword:password})});const d=await r.json();if(!r.ok)throw new Error(d.error);setPassword("");router.refresh();alert(`Withdrawal submitted. Reference: WD-${d.order.id}`)}catch(e){setError(e.message)}finally{setBusy(false)}}
./.audit-backup-warnings/WithdrawalFlow.jsx.bak:17: return <><form className="form-card" onSubmit={submit}><label>Choose amount</label><div className="amount-grid">{options.map(v=><button type="button" className={`amount-choice ${String(v)===String(amount)?'selected':''}`} key={v} onClick={()=>setAmount(String(v))}>GHS {Number(v).toLocaleString()}</button>)}</div>{!options.length&&<p className="muted">No withdrawal amounts are currently configured by Admin.</p>}<label>Funds Password<input value={password} onChange={e=>setPassword(e.target.value.replace(/\D/g,'').slice(0,6))} inputMode="numeric" maxLength={6} type="password" placeholder="6 numbers"/></label><div className="service-row"><span>Service charge</span><strong>GHS {feeValue.toFixed(2)}</strong></div><div className="service-row"><span>You receive</span><strong>GHS {net.toFixed(2)}</strong></div>{!policy?.allowed&&policy?.reason&&<p className="auth-error">{policy.reason}</p>}{error&&<p className="auth-error">{error}</p>}<button type="submit" className="primary-button" disabled={busy||!amount||password.length!==6||policy?.allowed!==true}>{busy?'Submitting…':'Submit Withdrawal'}</button></form><p className="muted">Processing time: {policy?.processingMinHours??0}–{policy?.processingMaxHours??48} hours.</p></>;
./audit-files.txt:2037:app/api/withdrawal-options/route.js:5:export async function GET(request){const s=session(request);if(!s)return Response.json({error:"Unauthorized"},{status:401});const db=getDb();const amounts=(await db.query("SELECT amount FROM withdrawal_amounts WHERE active=TRUE ORDER BY display_order,amount")).rows.map(r=>Number(r.amount));const p=await getWithdrawalPolicy(s.userId);return Response.json({amounts,allowed:p.allowed,reason:p.allowed?null:p.reason,feeMode:p.feeMode||null,feeValue:Number(p.feeValue||0),processingMinHours:Number(p.processingMinHours||0),processingMaxHours:Number(p.processingMaxHours||48),rankName:p.rankName||null});}
./withdrawal-files.txt:667:export async function GET(request){const s=session(request);if(!s)return Response.json({error:"Unauthorized"},{status:401});const db=getDb();const amounts=(await db.query("SELECT amount FROM withdrawal_amounts WHERE active=TRUE ORDER BY display_order,amount")).rows.map(r=>Number(r.amount));const p=await getWithdrawalPolicy(s.userId);return Response.json({amounts,allowed:p.allowed,reason:p.allowed?null:p.reason,feeMode:p.feeMode||null,feeValue:Number(p.feeValue||0),processingMinHours:Number(p.processingMinHours||0),processingMaxHours:Number(p.processingMaxHours||48),rankName:p.rankName||null});}
./withdrawal-files.txt:694:   return Response.json({error:"Enter a valid withdrawal amount."},{status:400});
./withdrawal-files.txt:724:    SELECT 1 FROM withdrawal_amounts
./withdrawal-files.txt:729:    throw new Error("This withdrawal amount is not currently available.");
./withdrawal-files.txt:733:  if(net<=0) throw new Error("Withdrawal amount after fee must be greater than zero.");
./withdrawal-files.txt:764:  `,[s.userId,value,fee,`WD-${order.rows[0].id}`,JSON.stringify({withdrawalOrderId:order.rows[0].id,netAmount:net})]);
./withdrawal-files.txt:769:    VALUES($1,$2,'withdrawal_hold',$3,$4,'Withdrawal amount reserved')
./withdrawal-files.txt:799: async function load(){try{const [a,o,v]=await Promise.all([fetch('/api/account'),fetch('/api/withdrawal-options'),fetch('/api/account-security')]);const ad=await a.json(),od=await o.json(),vd=await v.json();if(a.ok)setHasFundsPassword(!!ad.account?.has_funds_password);if(o.ok){setOptions(od.amounts||[]);setPolicy(od);}if(v.ok){setIdentity(vd.verification||null);setGovName(vd.verification?.government_name||"");setGovId(vd.verification?.government_id_number||"");}}catch(e){setError("Unable to load withdrawal settings.")}}
./withdrawal-files.txt:803: async function submit(e){e.preventDefault();setError("");setBusy(true);try{const r=await fetch('/api/withdrawals',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({amount,fundsPassword:password})});const d=await r.json();if(!r.ok)throw new Error(d.error);setPassword("");router.refresh();alert(`Withdrawal submitted. Reference: WD-${d.order.id}`)}catch(e){setError(e.message)}finally{setBusy(false)}}
./withdrawal-files.txt:807: return <><form className="form-card" onSubmit={submit}><label>Choose amount</label><div className="amount-grid">{options.map(v=><button type="button" className={`amount-choice ${String(v)===String(amount)?'selected':''}`} key={v} onClick={()=>setAmount(String(v))}>GHS {Number(v).toLocaleString()}</button>)}</div>{!options.length&&<p className="muted">No withdrawal amounts are currently configured by Admin.</p>}<label>Funds Password<input value={password} onChange={e=>setPassword(e.target.value.replace(/\D/g,'').slice(0,6))} inputMode="numeric" maxLength={6} type="password" placeholder="6 numbers"/></label><div className="service-row"><span>Service charge</span><strong>GHS {feeValue.toFixed(2)}</strong></div><div className="service-row"><span>You receive</span><strong>GHS {net.toFixed(2)}</strong></div>{!policy?.allowed&&policy?.reason&&<p className="auth-error">{policy.reason}</p>}{error&&<p className="auth-error">{error}</p>}<button type="submit" className="primary-button" disabled={busy||!amount||password.length!==6||policy?.allowed!==true}>{busy?'Submitting…':'Submit Withdrawal'}</button></form><p className="muted">Processing time: {policy?.processingMinHours??0}–{policy?.processingMaxHours??48} hours.</p></>;
./withdrawal-files.txt:835:app/api/withdrawal-options/route.js:5:export async function GET(request){const s=session(request);if(!s)return Response.json({error:"Unauthorized"},{status:401});const db=getDb();const amounts=(await db.query("SELECT amount FROM withdrawal_amounts WHERE active=TRUE ORDER BY display_order,amount")).rows.map(r=>Number(r.amount));const p=await getWithdrawalPolicy(s.userId);return Response.json({amounts,allowed:p.allowed,reason:p.allowed?null:p.reason,feeMode:p.feeMode||null,feeValue:Number(p.feeValue||0),processingMinHours:Number(p.processingMinHours||0),processingMaxHours:Number(p.processingMaxHours||48),rankName:p.rankName||null});}
./withdrawal-files.txt:837:app/api/withdrawals/route.js:25:   return Response.json({error:"Enter a valid withdrawal amount."},{status:400});
./withdrawal-files.txt:841:app/api/withdrawals/route.js:55:    SELECT 1 FROM withdrawal_amounts
./withdrawal-files.txt:842:app/api/withdrawals/route.js:60:    throw new Error("This withdrawal amount is not currently available.");
./withdrawal-files.txt:843:app/api/withdrawals/route.js:64:  if(net<=0) throw new Error("Withdrawal amount after fee must be greater than zero.");
./withdrawal-files.txt:844:app/api/withdrawals/route.js:74:  // Reserve the gross amount immediately; no second withdrawal can spend it.
./withdrawal-files.txt:847:app/api/withdrawals/route.js:95:  `,[s.userId,value,fee,`WD-${order.rows[0].id}`,JSON.stringify({withdrawalOrderId:order.rows[0].id,netAmount:net})]);
./withdrawal-files.txt:848:app/api/withdrawals/route.js:100:    VALUES($1,$2,'withdrawal_hold',$3,$4,'Withdrawal amount reserved')
./withdrawal-files.txt:939:app/components/WithdrawalFlow.jsx:9: async function load(){try{const [a,o,v]=await Promise.all([fetch('/api/account'),fetch('/api/withdrawal-options'),fetch('/api/account-security')]);const ad=await a.json(),od=await o.json(),vd=await v.json();if(a.ok)setHasFundsPassword(!!ad.account?.has_funds_password);if(o.ok){setOptions(od.amounts||[]);setPolicy(od);}if(v.ok){setIdentity(vd.verification||null);setGovName(vd.verification?.government_name||"");setGovId(vd.verification?.government_id_number||"");}}catch(e){setError("Unable to load withdrawal settings.")}}
./withdrawal-files.txt:940:app/components/WithdrawalFlow.jsx:13: async function submit(e){e.preventDefault();setError("");setBusy(true);try{const r=await fetch('/api/withdrawals',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({amount,fundsPassword:password})});const d=await r.json();if(!r.ok)throw new Error(d.error);setPassword("");router.refresh();alert(`Withdrawal submitted. Reference: WD-${d.order.id}`)}catch(e){setError(e.message)}finally{setBusy(false)}}
./withdrawal-files.txt:943:app/components/WithdrawalFlow.jsx:17: return <><form className="form-card" onSubmit={submit}><label>Choose amount</label><div className="amount-grid">{options.map(v=><button type="button" className={`amount-choice ${String(v)===String(amount)?'selected':''}`} key={v} onClick={()=>setAmount(String(v))}>GHS {Number(v).toLocaleString()}</button>)}</div>{!options.length&&<p className="muted">No withdrawal amounts are currently configured by Admin.</p>}<label>Funds Password<input value={password} onChange={e=>setPassword(e.target.value.replace(/\D/g,'').slice(0,6))} inputMode="numeric" maxLength={6} type="password" placeholder="6 numbers"/></label><div className="service-row"><span>Service charge</span><strong>GHS {feeValue.toFixed(2)}</strong></div><div className="service-row"><span>You receive</span><strong>GHS {net.toFixed(2)}</strong></div>{!policy?.allowed&&policy?.reason&&<p className="auth-error">{policy.reason}</p>}{error&&<p className="auth-error">{error}</p>}<button type="submit" className="primary-button" disabled={busy||!amount||password.length!==6||policy?.allowed!==true}>{busy?'Submitting…':'Submit Withdrawal'}</button></form><p className="muted">Processing time: {policy?.processingMinHours??0}–{policy?.processingMaxHours??48} hours.</p></>;
./withdrawal-files.txt:966:lib/transaction-rules.js:14:  if (!Number.isInteger(amountPesewas) || amountPesewas <= 0) return "Invalid withdrawal amount.";
./withdrawal-files.txt:971:lib/transaction-service.js:88:export async function createWithdrawal({ userId, amount, fee, metadata = {} }) {
