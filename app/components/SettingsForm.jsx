"use client";
import {useState} from "react";
export default function SettingsForm(){const [language,setLanguage]=useState("English");const [notifications,setNotifications]=useState(true);const [status,setStatus]=useState("");async function save(){setStatus("");try{const r=await fetch('/api/preferences',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({language,notificationsEnabled:notifications})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to save settings.');setStatus('Settings saved successfully.')}catch(e){setStatus(e.message)}}return <section className="admin-card"><div className="admin-card-head"><strong>Preferences</strong></div><div className="form-card"><label>Language<select value={language} onChange={e=>setLanguage(e.target.value)}><option>English</option></select></label><label className="check-row"><input type="checkbox" checked={notifications} onChange={e=>setNotifications(e.target.checked)}/> Notifications</label>{status&&<p className="muted">{status}</p>}<button type="button" className="primary-button" onClick={save}>Save Settings</button></div></section>}
